# E-Market — Frontend

Технологии: React + TypeScript + Tailwind CSS.

Запуск локально:
1. npm install
2. npm run dev
3. npm run build

Env:
- Создай .env.local (не хранить в репо). См. .env.example (если есть).
