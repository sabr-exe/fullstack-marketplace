# E-Market — Backend

Технологии: Django, DRF.

Запуск локально:
1. python -m venv .venv
2. source .venv/bin/activate
3. pip install -r requirements.txt
4. cp .env.example .env && настроить .env
5. ./manage.py migrate
6. ./manage.py runserver
